import * as React from 'react';
import {
  StyleSheet,
  SafeAreaView,
  Text,
  ScrollView,
  TouchableOpacity,
  View,
} from 'react-native';

function LearningPage() {
  return (
    <SafeAreaView>
      <Tex>Learn</Tex>
    </SafeAreaView>
  );
}

export default LearningPage;
